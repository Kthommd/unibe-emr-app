# Example environment variables. Copy this file to `.env` and modify values
# as needed. Variables prefixed with `VITE_` will be exposed to the client.

# Base URL for any external API the app may call
VITE_API_BASE_URL=https://api.example.com
